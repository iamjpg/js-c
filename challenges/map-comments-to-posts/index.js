/*
 * Task:
 * In the services file, import the calls to return both all posts and all comments.
 * They return two sepearate arrays of objects, respectively.
 *
 * Take both responses and merge the two into an array of objects containing
 * the posts and corresponding comments object for the posts.
 *
 * Output the array of objects to the DOM using JSON.stringify.
 *
 * The array of object should look like the JSON in expected-output.json
 */

const outputContainer = document.querySelector('#pre');
